# Codex app-server protocol snapshot

Generated from the installed `codex-cli 0.146.0` on 2026-08-12:

```powershell
codex app-server generate-json-schema --out electron/core/codex-protocol-schema --experimental
```

The runtime adapter intentionally handles a narrow, fail-closed subset of this version-matched contract. Regenerate and review this snapshot whenever the supported Codex CLI version changes.
