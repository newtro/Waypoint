import {describe,expect,it} from 'vitest'
import {assertRoute,proposeRoute} from './provider-routing.js'
const ready=[{name:'codex' as const,available:true,compatible:true,version:'0.146.0'},{name:'claude' as const,available:true,compatible:true,version:'2.1.220'}]
describe('local provider routing',()=>{
  it('honors preference, reports attachment bounds, and keeps fallback off',()=>{const route=proposeRoute({capabilities:ready,preferred:'claude',allowFallback:false,securityProfileId:'profile-a',attachments:[{id:'text',mediaType:'text/plain',bytes:10},{id:'large',mediaType:'text/plain',bytes:512*1024+1},{id:'image',mediaType:'image/png',bytes:10},{id:'pdf',mediaType:'application/pdf',bytes:10}]});expect(route).toMatchObject({selected:'claude',eligible:['codex','claude'],fallback:[],fallbackEnabled:false,device:'local'});expect(route.providers.find((item)=>item.provider==='claude')).toMatchObject({deliverableAttachmentIds:['text'],localOnlyAttachmentIds:['large','image','pdf']});expect(()=>assertRoute(route,'claude','profile-a')).not.toThrow()})
  it('fails closed without fallback and uses only eligible local CLI when opted in',()=>{const capabilities=[{name:'codex' as const,available:false,error:'missing'},{name:'claude' as const,available:true,compatible:true,version:'2.1.220'}],closed=proposeRoute({capabilities,preferred:'codex',allowFallback:false,securityProfileId:'p',attachments:[]}),open=proposeRoute({capabilities,preferred:'codex',allowFallback:true,securityProfileId:'p',attachments:[]});expect(closed.selected).toBeUndefined();expect(open).toMatchObject({selected:'claude',fallback:[]});expect(()=>assertRoute(open,'codex','p')).toThrow(/not eligible/)})
  it('rejects incompatible CLIs and profile substitution',()=>{const route=proposeRoute({capabilities:[{name:'codex',available:true,compatible:false,compatibilityError:'stale'},{name:'claude',available:false}],preferred:'codex',allowFallback:true,securityProfileId:'p',attachments:[]});expect(route.selected).toBeUndefined();expect(route.providers[0].reason).toBe('stale');expect(()=>assertRoute({...route,selected:'codex'},'codex','other')).toThrow(/not eligible/)})
})
