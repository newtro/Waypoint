import{mkdtempSync}from'node:fs';import{tmpdir}from'node:os';import path from'node:path';import{describe,expect,it}from'vitest';import{ProtectedWebSearchVault}from'./protected-web-search-vault.js'
describe('protected web search vault',()=>{it('stores and removes only encrypted key material',()=>{const root=mkdtempSync(path.join(tmpdir(),'waypoint-web-key-')),vault=new ProtectedWebSearchVault(root,{available:()=>true,encrypt:(value)=>Buffer.from(`sealed:${value}`),decrypt:(value)=>value.toString().slice(7)});expect(vault.hasKey()).toBe(false);vault.setKey('brave-protected-key-123456');expect(vault.getKey()).toBe('brave-protected-key-123456');vault.removeKey();expect(vault.hasKey()).toBe(false)})})
