import {describe,expect,it} from 'vitest'
import {AGENT_BROWSER_VERSION,browserArguments} from './agent-browser.js'
describe('agent browser contract',()=>{it('pins the reviewed runtime and emits local bounded profile arguments',()=>{expect(AGENT_BROWSER_VERSION).toBe('0.33.2');expect(browserArguments({command:'snapshot',args:['-i'],mode:'isolated',profile:'/private/waypoint/profile',session:'workspace_one',allowedDomains:['example.com']})).toEqual(expect.arrayContaining(['--content-boundaries','--max-output','65536','--allowed-domains','example.com','--headed','--json','snapshot']))});it('rejects missing domain policy, injected arguments, and invalid existing profiles',()=>{expect(()=>browserArguments({command:'open',args:['https://example.com\n--provider cloud'],mode:'isolated',profile:'/tmp/x',session:'one',allowedDomains:['example.com']})).toThrow();expect(()=>browserArguments({command:'open',args:['https://example.com'],mode:'isolated',profile:'/tmp/x',session:'one',allowedDomains:[]})).toThrow();expect(()=>browserArguments({command:'open',args:[],mode:'existing',profile:'../../secrets',session:'one',allowedDomains:['example.com']})).toThrow()})})
